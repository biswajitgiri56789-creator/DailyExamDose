name: Daily Exam Dose Bot

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  send-question:
    runs-on: ubuntu-latest
    steps:
      - name: Send Question
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          curl -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
          -d "chat_id=$CHAT_ID" \
          -d "text=📚 *Daily Exam Dose*

🎯 Question #1
📖 Subject: Physics
🏫 Class: 11

❓ What is Newton's First Law?

A. Law of Inertia
B. F=ma
C. Action-Reaction
D. Law of Gravity

✅ Answer: A" \
          -d "parse_mode=Markdown"
