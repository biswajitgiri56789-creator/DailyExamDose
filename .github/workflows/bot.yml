name: Telegram Bot

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */1 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: Send Question
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
          -d "chat_id=$CHAT_ID" \
          -d "text=📚 *Daily Exam Dose*

✅ Question 1
📖 Subject: Physics
🏫 Class: 11

❓ What is force?

A. Mass × Acceleration
B. Mass ÷ Acceleration
C. Mass + Acceleration
D. Mass - Acceleration

✅ Answer: A" \
          -d "parse_mode=Markdown"
